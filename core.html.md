# pshnb IPython magic


<!-- WARNING: THIS FILE WAS AUTOGENERATED! DO NOT EDIT! -->

``` python
__file__ = './00_core.ipynb'
```

``` python
env = dict(os.environ, TERM='dumb', PS1='$', PS2='$')
eshell = os.environ['SHELL']
sh = pexpect.spawn(eshell, encoding='utf-8', env=env)
```

``` python
env = dict(os.environ, TERM='dumb', PS1='', PS2='')
eshell = os.environ['SHELL']
sh = pexpect.spawn(eshell, encoding='utf-8', env=env)
sh.sendline('stty -echo')
sh.readline()
echo = os.urandom(8).hex()
echo_re = re.compile(fr'^{echo}\s*$', flags=re.MULTILINE)
sh.sendline(f'export PS1=""')
sh.sendline('set +o vi +o emacs')
sh.sendline('echo '+echo)
sh.expect(echo_re, timeout=2)
```

    0

``` python
sh.sendline('ls | head -3')
sh.sendline('echo '+echo)
sh.expect(echo_re, timeout=2)
print(sh.before)
```

    00_core.ipynb
    CHANGELOG.bak
    CHANGELOG.md

------------------------------------------------------------------------

<a
href="https://github.com/answerdotai/pshnb/blob/main/pshnb/core.py#L21"
target="_blank" style="float:right; font-size:smaller">source</a>

### ShellInterpreter

>  ShellInterpreter (debug=False, timeout=2, shell_path=None, sudo=False,
>                        dumb=False)

*Initialize self. See help(type(self)) for accurate signature.*

``` python
sh = ShellInterpreter()
print(sh('ls | head -3'))
```

    00_core.ipynb
    CHANGELOG.bak
    CHANGELOG.md

``` python
sh = ShellInterpreter(sudo=True)

sh('cd')
print(sh('pwd'))
sh('cd ..')
print(sh('pwd'))
print(sh('whoami'))
```

    /var/root
    /var
    root

------------------------------------------------------------------------

<a
href="https://github.com/answerdotai/pshnb/blob/main/pshnb/core.py#L55"
target="_blank" style="float:right; font-size:smaller">source</a>

### shell_replace

>  shell_replace (s, shell=None)

*Replace `@{var}` refs in `s` with their variable values, if they exist*

``` python
b = 1

a = '''asdf
$@{b} @{aa}
fdsa'''

print(shell_replace(a))
```

    asdf
    $1 @{aa}
    fdsa

------------------------------------------------------------------------

<a
href="https://github.com/answerdotai/pshnb/blob/main/pshnb/core.py#L62"
target="_blank" style="float:right; font-size:smaller">source</a>

### PshMagic

>  PshMagic (shell, sudo=False, timeout=2, expand=True, o=None)

*Initialize self. See help(type(self)) for accurate signature.*

------------------------------------------------------------------------

<a
href="https://github.com/answerdotai/pshnb/blob/main/pshnb/core.py#L112"
target="_blank" style="float:right; font-size:smaller">source</a>

### create_magic

>  create_magic (shell=None)

``` python
# Only required if you don't load the extension
create_magic()
```

``` python
%psh pwd
```

    /Users/jhoward/Documents/GitHub/pshnb

``` python
%psh cd ..
```

``` python
%psh pwd
```

    /Users/jhoward/Documents/GitHub

``` python
%%psh
cat > tmp << EOF
hi
there
EOF
```

``` python
%psh cat tmp
```

    hi
    there

``` python
%psh rm tmp
```

``` python
%psh ls | head -3
```

    ContextKit
    FastHTML-Gallery
    aimagic

``` python
n = 2
```

``` python
%psh echo @{n}
```

    2

``` python
%psh ls | head -@{n}
```

    ContextKit
    FastHTML-Gallery

``` python
%%psh
echo starting
(sleep 1; echo finished) &
```

    starting
    [1] 99418

``` python
%psh
```

    finished

    [1]+  Done                    ( sleep 1; echo finished )

``` python
%psh -h
```

    ::

      %psh [-h] [-r] [-o] [-x] [-X] [-s] [-S] [-t TIMEOUT] [command ...]

    Run line or cell in persistent shell

    positional arguments:
      command               The command to run

    options:
      -h, --help            Show this help
      -r, --reset           Reset the shell interpreter
      -o, --obj             Return this magic object
      -x, --expand          Enable variable expansion
      -X, --no-expand       Disable variable expansion
      -s, --sudo            Enable sudo
      -S, --no-sudo         Disable sudo
      -t TIMEOUT, --timeout TIMEOUT
                            Set timeout in seconds

``` python
%psh pwd
```

    /Users/jhoward/Documents/GitHub

``` python
%psh -r
```

``` python
%psh pwd
```

    /Users/jhoward/Documents/GitHub/pshnb

``` python
%psh -s
```

``` python
%psh whoami
```

    root

``` python
%psh -S
```

``` python
%psh whoami
```

    jhoward

``` python
%psh -t 1
```

``` python
try: get_ipython().run_line_magic('psh', 'sleep 2')
except TIMEOUT: print("timed out")
```

    timed out

------------------------------------------------------------------------

<a
href="https://github.com/answerdotai/pshnb/blob/main/pshnb/core.py#L118"
target="_blank" style="float:right; font-size:smaller">source</a>

### load_ipython_extension

>  load_ipython_extension (ipython)

*Required function for creating magic*

------------------------------------------------------------------------

<a
href="https://github.com/answerdotai/pshnb/blob/main/pshnb/core.py#L123"
target="_blank" style="float:right; font-size:smaller">source</a>

### create_ipython_config

>  create_ipython_config ()

*Called by `pshnb_install` to install magic*
